<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Show all data</title>
		<link type="text/css" rel="stylesheet" href="../styles/data.css" />
	</head>
	<body class="main-container">
		<div class="profile-container">
			<div class="img-div">
				<h2>Profile Picture</h2><hr>
				<% try { %>
				<img
					src="../uploaded_file/<%= data[data.length-1].imgFileName %>"
					alt="Profile picture"
				/>
				<% } catch(e) { %>
				<p>Profile picture not found <%= e.message %></p>
				<% } %>
			</div>
			<h2>Profile information</h2><hr>
			<% try { %> <%# for (let i = 0; i < data.length; i++) { %>
			<h1><%= data[data.length-1].name %></h1>
			<p>Email: <%= data[data.length-1].email %></p>
			<p>Age: <%= data[data.length-1].age %></p>
			<%# } %> <% } catch (e) { %>
			<p>Enternal server error ⚠</p>
			<p><%= e %></p>
			<% } %>
		</div>
	</body>
</html>
